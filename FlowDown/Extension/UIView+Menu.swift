//
//  UIView+Menu.swift
//  FlowDown
//
//  Created by AI Assistant
//

import UIKit

extension UIView {
    @available(*, unavailable, message: "upgrade in progress")
    func present(
        menu _: UIMenu,
        anchorPoint _: CGPoint? = nil,
        controllerDidLoad _: @escaping (UIViewController) -> Void = { _ in },
        controllerDidPresent _: @escaping (UIViewController) -> Void = { _ in }
    ) {
        fatalError("upgrade in progress")
    }
}
